# 目前项目用的是npm


# 我现在想调整的是：通过拖拽，让被拖动的元素精准地显示在目标位置（比如插入到某个容器的某个位置，或作为某个子项的子节点）。

# 我的拖拽逻辑在useMaterialDrops，用的仓库是zustand，在stores文件里面，请基于 React DnD 最新版本（v14+） 生成代码

# 现在要实现的目标有：
1.（已经实现）希望在拖拽过程中就看到预览效果，比如：

一条插入线（placeholder line）
高亮某个区域
虚拟的“插入位置”框
在 useDrop 的 monitor.isOver() 和 canDrop 等状态中，动态改变 UI。

需要处理 CSS 定位、插入线逻辑、避免闪烁。

2.希望实现，更改属性的这个作用，要求修改完成属性之后属性可以正确显示大小，颜色也可以进行更改，如果有antd不支持的属性请先删除。